{
  "id": "sift",
  "name": "Sift",
  "description": "Token-efficient capture, stage, and Notion sync tools for long-form research workflows.",
  "version": "0.1.1",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "artifactRoot": {
        "type": "string",
        "description": "Optional absolute or workspace-relative root for staged artifacts."
      },
      "notion": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "apiKey": {
            "type": "string"
          },
          "apiVersion": {
            "type": "string"
          }
        }
      }
    }
  },
  "uiHints": {
    "artifactRoot": {
      "label": "Artifact root",
      "help": "Optional override for where staged artifacts are written."
    },
    "apiKey": {
      "label": "Notion API key",
      "sensitive": true,
      "placeholder": "ntn_..."
    },
    "apiVersion": {
      "label": "Notion API version",
      "placeholder": "2025-09-03"
    }
  }
}
